fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
python-multipart>=0.0.20
aiosqlite>=0.20,<1.0
sse-starlette>=2.0,<3.0
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
google-cloud-texttospeech>=2.31,<3.0
# Edge-TTS — Microsoft Edge's TTS service via Python, no browser or API key.
# Default provider for users who want read-aloud without any cloud setup.
edge-tts>=6.1,<7.0
# AES-encrypted ZIPs (backup export/import with password).
# Drop-in for stdlib zipfile; without a password it behaves like zipfile.
pyzipper>=0.3,<1.0
claude-agent-sdk>=0.1.81,<0.2
anthropic>=0.40,<1.0
