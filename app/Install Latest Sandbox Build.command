#!/usr/bin/env bash
# Veraltete projekt-spezifische Variante.
# Die generische Variante liegt jetzt unter ~/Projects/Install Latest Sandbox Builds.command
# und installiert alle Sandbox-Builds aus ~/Projects/.sandbox-builds/.
open "$(dirname "$0")/../Install Latest Sandbox Builds.command"
