#!/usr/bin/env bash
# Veraltet — die generische Variante liegt unter ~/Projects/Setup Sandbox-Auto-Installer.command
# und beobachtet alle Android-Projekte zentral.
open "$(dirname "$0")/../../Setup Sandbox-Auto-Installer.command"
